package br.com.rizermarketplaces.core.marketplace.model;

// Enum que define domínios (realms) de negócio dos produtos. Usado para diferenciar
// tipos de catálogo e permitir regras/atributos por domínio.
public enum ProductRealm {
    VEHICLES,
    REAL_ESTATE,
    SERVICES
}
